import React from "react";

import CallButton from "../../components/CallButton";

export default {
  title: "CallButton",
  component: CallButton,
};

const Template = () => <CallButton />;

export const Primary = Template.bind({});
